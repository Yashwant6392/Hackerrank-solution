string fairRations(vector<int> B) {

int n=B.size();
int count=0;
for(int i=0;i<n;i++)
{
    if(B[i]%2!=0)
    {
        B[i]++;
        B[i+1]++;
        count+=2;
    }
    if(B[n-1]%2!=0)
    return "NO";   
}
return to_string(count);  //convert integer to string
}
